# supervised_learning_app
An AutoML-inspired supervised learning desktop application built with Python and Tkinter. Upload training and test CSV files and the application automatically trains multiple machine learning models, selects the best-performing algorithm, generates predictions, and exports both output.csv and a detailed performance report.
